// OPTIONS /.well-known/oauth-authorization-server (handleOptions) is not tested:
// this is a CORS preflight handler, not business logic — it is handled transparently
// by the browser and does not require API-level test coverage.
